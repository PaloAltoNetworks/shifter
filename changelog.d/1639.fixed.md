Fixed portal ASG instance refreshes intermittently wedging on a transient instance reporting "insufficient data to evaluate its health with Amazon EC2." The ASG health-check type was `EC2`, so refresh readiness tracked EC2 status checks rather than real application readiness. It now defaults to `ELB`, tying instance and refresh health to the ALB target group so a refresh converges when the portal is actually serving.
