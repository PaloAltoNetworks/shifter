Hardened portal auth and audit trust: dev-login admission is now bound to the direct peer address (loopback/admin CIDRs) instead of the spoofable Host header, audit source IPs are resolved from the trusted rightmost X-Forwarded-For hop rather than the client-controlled leftmost value, and every self-service `user_type` change now writes a fail-closed, reviewable audit row for the resulting CTF group membership.
