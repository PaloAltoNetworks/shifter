Reduced the cyclomatic complexity of `_content_ref_from_resource` in the ACES content-delivery prep module by extracting the `spec` field-coercion into a `_spec_str` helper, satisfying the SonarCloud complexity gate. No behavior change.
