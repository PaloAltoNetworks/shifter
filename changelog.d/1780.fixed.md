Fixed the Ubuntu base-image bake, which aborted with `python3-boto3 has no installation candidate`. The VPN gateway runtime install now enables the `universe` apt component and refreshes the package lists before installing `openvpn`/`python3-boto3`, since the cloud image does not always ship with `universe` enabled.
