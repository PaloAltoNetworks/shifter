CTF flag-submission concurrency tests (duplicate-solve, attempt-limit lockout, submission cooldown) now run against a real PostgreSQL backend in CI, proving `submit_flag()`'s row-lock guard serializes concurrent requests.
