Enabled participant and operator portal access (browser SSH terminal and Guacamole SSH/RDP) to scenario endpoints inside GCP VM range cells, at parity with the existing AWS range access, by authorizing the portal and guacd workloads to reach range guests over the private range network.
