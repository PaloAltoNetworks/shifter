Range instance visibility is configurable per event (#483): organizers choose which instance OS types participants see in the terminal (defaulting to attacker boxes only, empty meaning all), replacing the hardcoded Kali-only rule via a pluggable shared visibility policy.
