Surfaced read-only ACES participant/runtime and access-channel projection fields (`aces_participant_runtime`) on the Mission Control range read responses and dashboard, sibling to the existing `aces_projection` and absent for legacy/non-ACES ranges.
