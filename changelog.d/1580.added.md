Resolve authored RAES artifact requirements against backend capabilities: range launch resolves each node's artifact requirement to a concrete backend-owned image and fences it onto the operation input, so provisioning realizes the exact admitted artifact and fails closed when a requirement cannot be satisfied (#1580).
