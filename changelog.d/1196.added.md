**OSS-release hygiene: community files, dependency automation, CI hardening, and identifier strip.** Added `SECURITY.md` (PANW PSIRT routing), `SUPPORT.md` (community-supported policy), and a project-specific `CONTRIBUTING.md`. Added `.github/dependabot.yml` enumerating every uv, npm, and github-actions package root in the repo. Added `.github/ISSUE_TEMPLATE/` (bug + feature) with `config.yml` redirecting security disclosures to PSIRT. Added CodeQL workflow (`security-extended` suite, least-privilege permissions, no `pull_request_target`) and a PR-title-lint workflow validating conventional-commit titles. Stripped the SonarCloud project token from `README.md` badges and removed its `.gitleaks.toml` allowlist entries. Replaced committed `keplerops.com` references and the hardcoded `bedwards@paloaltonetworks.com` alarm/admin email with `example.com` placeholders so an OSS deployer sees them as "replace me before deploy" rather than working defaults. Untracked the previously-committed `.playwright-mcp/` browser-session leftovers (already in `.gitignore`).
