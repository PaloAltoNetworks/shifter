Migrate Mission Control JSON endpoints to the shared DRF `/api/v1/mission-control/` surface with scoped platform API-token access and legacy route compatibility, and keep the IAM role naming guardrail stable on all-files runs.
