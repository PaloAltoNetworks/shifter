### Security
- Remove hardcoded `YOUR_EMAIL@example.com` budget-alert placeholders from AWS core Terraform; alert recipients now flow through per-environment `budget_alert_email` deploy secrets (`TF_VARS_*_CORE`) and gitignored `local.auto.tfvars`.
- Add ADR guard checks that block operational Terraform placeholders and `AdministratorAccess` on the GitHub Actions OIDC role from re-entering the repo.
