Repointed the `dev` pre-promoted Domain Controller AMI in `shifter/packer/dc-amis.json` to the account-owned `ami-02fd5d81a22ba4eef` so `/shifter/ami/dc` can be seeded in the rebuilt aws-dev account (the previous id was deregistered during teardown, which blocked `packer.yml ami_type=dc` and the portal Terraform plan).
