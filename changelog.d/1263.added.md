Added an automated ACES backend conformance gate that validates Shifter's published `provisioning-only` backend manifest (builder and checked-in artifact) through ACES-owned conformance tooling, with bounded, sanitized diagnostics.
