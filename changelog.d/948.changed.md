### Changed

- Portal settings now require an explicit `ENVIRONMENT` at boot instead of silently defaulting to `production`, log resolved posture for each subsystem at startup, and ship a generated env-var manifest as the deploy contract (#948).
