Exclude the dev dependency group from the production portal image build (`uv export --no-dev`). Dev/test tooling—and the dev-only `aces-sdl` git dependency, which uv cannot emit a hash for—no longer leak into the image, so the hash-pinned `--require-hashes` install (and the `Stack smoke (built image)` CI job) no longer fails.
