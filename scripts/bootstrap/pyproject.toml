[project]
name = "bootstrap"
version = "0.1.0"
description = "Shifter AWS account bootstrap deployment scripts"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-mock>=3.15.1",
]
