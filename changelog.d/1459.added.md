Add `scripts/ctfd-workshop/standup-gcp-ctfd.sh`, a small re-runnable gcloud helper that stands up the standalone CTFd workshop on GCE (static IP + 80/443 firewall + debian-12 VM running CTFd via docker compose behind host nginx), mirroring the AWS `ctfd-workshop` pattern for the GCP Polaris event surface.
