### Security
- Standardize deploy-managed AWS IAM role and instance profile names under the `shifter-*` prefix via an `iam_name_prefix` seam, and narrow GitHub Actions OIDC `iam_scoped` permissions to that namespace with a managed-policy attachment allowlist (#253).
