[project]
name = "lambda"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.42.7",
    "psycopg>=3.3.2",
]

[dependency-groups]
dev = [
    "moto>=5.1.18",
    "pytest>=9.0.2",
    "pytest-mock>=3.15.1",
]
