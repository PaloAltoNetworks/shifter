Repaired the CTF participant scoreboard "You" row highlight (the view now supplies `participant_id` to the row partial and the auto-refresh script) and added an own-row solve-history drill-down showing a participant's correct solves (challenge, category, points, solve time).
