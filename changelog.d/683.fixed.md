Fixed CTF credential emails failing to send: `send_credentials` reversed a
nonexistent `ctf:ctf_range` route, so every credentials batch was counted as
failed. The email now links to the participant range page
(`ctf:participant_range`). Surfaced by converting the notification service
tests from internal mocks to behavioral coverage during the #683 god-module
decomposition.
