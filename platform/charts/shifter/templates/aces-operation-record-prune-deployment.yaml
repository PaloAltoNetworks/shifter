apiVersion: apps/v1
kind: Deployment
metadata:
  name: aces-operation-record-prune
  namespace: {{ .Values.namespaces.platform }}
  labels:
    {{- include "shifter.partOfLabels" . | nindent 4 }}
    app.kubernetes.io/component: aces-operation-record-prune
spec:
  replicas: {{ .Values.workers.acesOperationRecordPrune.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/part-of: shifter
      app.kubernetes.io/component: aces-operation-record-prune
  template:
    metadata:
      labels:
        {{- include "shifter.partOfLabels" . | nindent 8 }}
        app.kubernetes.io/component: aces-operation-record-prune
      annotations:
        checksum/runtime-config: {{ include "shifter.runtimeConfigChecksum" . }}
    spec:
      {{- include "shifter.podSecurityContext" . | nindent 6 }}
      automountServiceAccountToken: false
      serviceAccountName: {{ .Values.serviceAccounts.workers.name }}
      containers:
        - name: aces-operation-record-prune
          image: {{ include "shifter.portalImage" . }}
          imagePullPolicy: {{ .Values.images.portal.pullPolicy }}
          {{- include "shifter.containerSecurityContextApp" . | nindent 10 }}
          {{- include "shifter.tmpVolumeMount" . | nindent 10 }}
          args: ["python", "manage.py", "run_aces_operation_record_prune"]
          envFrom:
            - configMapRef:
                name: platform-runtime
          livenessProbe:
            exec:
              command: ["sh", "-c", "find /tmp/aces-operation-record-prune-heartbeat -mmin -2 | grep -q ."]
            initialDelaySeconds: 90
            periodSeconds: 30
          resources:
            {{- toYaml .Values.workers.acesOperationRecordPrune.resources | nindent 12 }}
      {{- include "shifter.tmpVolume" . | nindent 6 }}
