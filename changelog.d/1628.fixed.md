Removed the dead `ctf-*` (`ctf-webshell`, `ctf-mailroom`, `ctf-helpdesk`, `ctf-devbox`, `ctf-vault`) `ami_type` choices from the `Packer AMI Build` workflow and the matching usage text in `scripts/ami.sh`. No `*.pkr.hcl` source backed them, so dispatching one ran `packer build -only='*.ctf-<x>'`, matched no build, and failed.
