CTF participant magic links now remain reusable through the event end instead of expiring after the default short TTL while the CTF is still active, with `MAGIC_LINK_EVENT_MAX_EXPIRY_HOURS` available for operators who need a stricter event-link ceiling.
