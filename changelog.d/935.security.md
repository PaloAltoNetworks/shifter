**Deploy workflows no longer route pull requests to self-hosted deploy runners.** AWS/GCP deploy jobs are push or manual-dispatch only, prod AWS deploys can be protected through the `aws-prod` GitHub Environment, and AWS ECR deploys now consume immutable digest-pinned images.
