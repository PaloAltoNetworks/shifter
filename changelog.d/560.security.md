ADR guard now blocks hardcoded CTF flag literals (`FLAG{...}`) in Mission Control runtime code (`no-mission-control-flag-literals`, ADR-004-R16), keeping challenge answers in the CTF/CTFd content domain rather than the application path.
