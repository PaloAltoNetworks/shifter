CTFd-compatible challenge packs import and export (CTF-1104): CTFd JSON entries map to Shifter challenges (plaintext flags hashed on import, hints with costs); exports omit unrecoverable flag values and Shifter-only fields.
