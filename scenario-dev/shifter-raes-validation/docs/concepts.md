# Shifter RAES validation

This pack provides one VM on one network for the native provisioning cutover
evidence command. The VM carries one empty-directory content placement, one
public-key local account, and one digest-bound configuration feature. The
combination keeps the pack small while requiring the live validation command to
observe verified evidence for every composition resource kind instead of
accepting topology alone.
