# Example deployment override for the prod core environment.
#
# Copy to `local.auto.tfvars` (gitignored) or set the `TF_VARS_PROD_CORE` GitHub
# Actions secret to this HCL before deploy. Terraform auto-loads `*.auto.tfvars`
# over the committed baseline.

budget_alert_email = "ops@your-domain.example"
