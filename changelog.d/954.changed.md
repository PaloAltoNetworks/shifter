Quality routing now runs validation by default for pull requests and `dev` pushes, with ordinary docs-only diffs as the only general skip path and guardrail documentation still treated as quality-relevant.
