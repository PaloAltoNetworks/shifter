Moved the CTF invite magic-link token off the URL query string. Invite emails now carry the token in the URL fragment (`#token=`), and a CSRF-protected token-exchange endpoint consumes it, keeping the credential out of access logs, the request formatter, and `Referer` headers (SonarCloud S8435).
