Mission Control ranges now expose their remaining server-enforced lifetime in the SPA, support bounded owner-requested extensions up to a fixed maximum, and offer a scoped OpenVPN profile download when the provisioned range supports it. CTF and Mission Control automatic teardown now share the same persisted range-lease reconciler.
