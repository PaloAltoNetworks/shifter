Portal WebSocket connections (terminal SSH sessions, range-status and notification sockets) now work in the production container image. The Gunicorn/Uvicorn ASGI workers were missing a WebSocket protocol backend, so the built image rejected every WebSocket upgrade (falling through to a 301) while `/health` still returned 200 — a container-only regression the new built-image stack smoke catches.
