Bulk participant import is now partial-success (CTF-603): bad rows (format errors, duplicates in the file or against the event) are skipped and reported per row while the rest import, and the SPA gained a CSV import dialog.
