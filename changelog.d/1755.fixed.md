**AWS range provisioning now grants the narrowly scoped IAM and load balancer
permissions required to create per-range OpenVPN gateways.** Policy guardrails
also reject broader permission variants, and post-deploy smoke tests stop
immediately when a range reaches a terminal failure state.
