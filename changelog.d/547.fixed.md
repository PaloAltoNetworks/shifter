### Fixed

- CTF flag submission cooldown rejections now return retry timing in the JSON API response, and the participant challenge page shows a rate-limit message instead of treating HTTP 429 as an incorrect flag (CTF-114).
