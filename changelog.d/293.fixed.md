Engine range status-update handler now refreshes `updated_at` when persisting status changes from pub/sub events, instead of leaving the timestamp stale because `save(update_fields=...)` bypassed Django's `auto_now`.
