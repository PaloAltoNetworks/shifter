Audit logging for authentication and terminal-session events: OIDC callback token-validation failures are now recorded as failed logins with a bounded reason, browser logout is audited, and terminal sessions that end on idle/max-duration timeout are distinguishable from ordinary closes (with the user email attached to session-lifecycle rows).
