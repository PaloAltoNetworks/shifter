### Changed

- Revoke residual `provisioner_lambda` database grants outside the reviewed cyberscript allowlist and remove the provisioner's range-event outbox write path and event-topic runtime binding (#1839).
