# Shifter ACES validation

This pack provides one VM on one network for the native provisioning cutover
evidence command. It is deliberately minimal so successful evidence cannot be
mistaken for a scenario-specific realization shortcut.
