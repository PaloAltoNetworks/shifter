SSHExecutor no longer logs raw PAN-OS command text or raw/cleaned device output, closing a clear-text-credentials-in-logs path (CWE-532). Command and output visibility is now byte-count only, mirroring the NGFWExecutor discipline.
