The CTF scheduler (CTF-1001/#526) now retries transient task failures with exponential backoff, exposes an organizer task history with run-now on the monitoring page, and documents its intentionally one-shot, deployment-supervised model.
