apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-warm-pool-reconciler
  namespace: {{ .Values.namespaces.platform }}
  labels:
    {{- include "shifter.partOfLabels" . | nindent 4 }}
    app.kubernetes.io/component: worker-warm-pool-reconciler
spec:
  replicas: {{ .Values.workers.warmPoolReconciler.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/part-of: shifter
      app.kubernetes.io/component: worker-warm-pool-reconciler
  template:
    metadata:
      labels:
        {{- include "shifter.partOfLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker-warm-pool-reconciler
      annotations:
        checksum/runtime-config: {{ include "shifter.runtimeConfigChecksum" . }}
    spec:
      {{- include "shifter.podSecurityContext" . | nindent 6 }}
      automountServiceAccountToken: false
      serviceAccountName: {{ .Values.serviceAccounts.workers.name }}
      containers:
        - name: worker-warm-pool-reconciler
          image: {{ include "shifter.portalImage" . }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          {{- include "shifter.containerSecurityContextApp" . | nindent 10 }}
          {{- include "shifter.tmpVolumeMount" . | nindent 10 }}
          args: ["python", "manage.py", "reconcile_warm_pool", "--loop", "--interval", "300"]
          envFrom:
            - configMapRef:
                name: platform-runtime
          livenessProbe:
            exec:
              command: ["sh", "-c", "find /tmp/worker-warm-pool-reconciler-heartbeat -mmin -10 | grep -q ."]
            initialDelaySeconds: 90
            periodSeconds: 60
          resources:
            {{- toYaml .Values.workers.warmPoolReconciler.resources | nindent 12 }}
      {{- include "shifter.tmpVolume" . | nindent 6 }}
