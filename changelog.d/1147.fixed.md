Fixed a CTF challenge-file cap race: concurrent uploads could exceed `MAX_FILES_PER_CHALLENGE` and assign duplicate `order` values because the count check and the record create were not atomic. The cap re-check and order assignment now run under a challenge row lock after the S3 upload, with best-effort cleanup of the uploaded object on a lost race (#1147).
