Cloning a custom scenario in the CMS scenario editor now copies the full persisted definition payload instead of a hardcoded three-field subset, so new definition fields are not silently dropped on clone.
