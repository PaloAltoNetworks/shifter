CTF extension points (CTF-1401): external Django apps can register custom flag validators and scoring strategies from AppConfig.ready; registered types become valid model values and win dispatch, with no standalone plugin lifecycle.
