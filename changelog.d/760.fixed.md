Document and enforce the `[skip tests]` deployment bypass policy: deploy.yml hardcodes `skip_tests: false`, adr_guard rejects commit-message bypasses and architecture jobs gated on `inputs.skip_tests`, and CI/deployment docs describe the contract.
