Added a CI-time lint that statically scans provisioner setup plans and fails when a script or `stdin_input` contains an unrendered `{{word}}` template token that is not a declared render-context key. This catches the placeholder collision (e.g. a stray `{{end}}`/`{{range}}`) before it fails on a live range at provisioning time.
