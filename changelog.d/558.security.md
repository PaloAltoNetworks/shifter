### Security

- Portal settings now fail closed when production runtime config is missing for allowed hosts, database, OIDC, field encryption, or email backend selection; dev/test/build defaults remain explicit posture-gated exceptions.
