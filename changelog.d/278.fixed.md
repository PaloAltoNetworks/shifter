### Fixed

- Range provisioning and teardown ECS task identifiers are stored in separate fields so destroy no longer overwrites provisioning correlation data ([#278](https://github.com/Brad-Edwards/shifter/issues/278)).
