### Security

Commercial self-serve ranges can run in an explicit zero-egress posture (`settings.range_egress.mode: none`) where participant subnet route tables have no `0.0.0.0/0` route to Network Firewall, NAT, or the internet. Existing event allowlist behavior is unchanged (ADR-026).
