**Audit logging failures now mark portal audit health degraded instead of only logging and returning `None`.** Non-strict audit calls remain best-effort for the caller, strict audit calls still fail closed, and `/health` exposes degraded audit state through the existing coarse health-check surface.
