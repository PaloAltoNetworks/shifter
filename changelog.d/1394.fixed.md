Publish the range-events SNS topic ARN to the `${ssm_prefix}/range-events-topic-id` SSM parameter so portal instances wire `RANGE_EVENTS_TOPIC_ID` into the outbox drainer and reconciler. The parameter was never created, so `worker-outbox-drainer` crash-looped with `RANGE_EVENTS_TOPIC_ID is not configured` and range-event delivery via the outbox was down.
