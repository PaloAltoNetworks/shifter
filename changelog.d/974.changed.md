### Changed

- Enforced the protected-branch CI baseline by removing the `[skip tests]` bypass, adding an always-on pre-commit hygiene/secret-scan job to `PR Gate`, and running CodeQL for both `main` and `dev` PRs.
