CTF organizer participant add, CSV import, and generated seats now provision a registered account immediately instead of passing through a transient invited state, and participant-lifecycle wording (status counts, the login-info-sent timestamp, invitation email copy, and API fields) no longer implies an invitation-acceptance workflow that does not exist (#535).
