GCP bootstrap now sources the first Identity Platform operator credentials from the gcp-dev `local.auto.tfvars` overlay (rendered from GitHub secrets in CI) as the authoritative value, so a rotated operator password persists across redeploys instead of being lost.
