GCE range cells now resolve each legacy scenario `ami_key` through an exact, deployment-approved image profile, allowing generic Kali, Polaris, and TechVault scenarios to coexist without runtime ConfigMap swaps.
